@extends('layouts.app')
@section('title',$enum->label()) @section('header',$enum->label())
@section('content')
<div class="mb-6 flex flex-wrap items-end justify-between gap-4"><div><h2 class="page-title">{{$enum->label()}}</h2><p class="page-subtitle">รายการเอกสารทั้งหมด เอกสารที่ POSTED แล้วไม่สามารถแก้ไขได้</p></div><a class="btn-primary" href="{{route('documents.create',strtolower($enum->value))}}"><svg class="size-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-width="2" d="M12 5v14m7-7H5"/></svg>สร้างเอกสาร</a></div>
<div class="table-shell"><div class="table-wrap"><table class="data-table"><thead><tr><th>เลขที่เอกสาร</th><th>วันที่เอกสาร</th><th>สถานะ</th><th>ผู้สร้าง</th><th class="text-right">จัดการ</th></tr></thead><tbody>@forelse($documents as $d)<tr><td><a class="font-bold text-brand-600 hover:text-brand-700" href="{{route('documents.show',$d)}}">{{$d->document_no}}</a></td><td class="whitespace-nowrap">{{$d->document_date->format('d/m/Y')}}</td><td><span class="{{$d->status->value==='POSTED'?'badge-green':($d->status->value==='CANCELLED'?'badge-slate':'badge-amber')}}"><span class="mr-1.5 size-1.5 rounded-full {{$d->status->value==='POSTED'?'bg-emerald-500':($d->status->value==='CANCELLED'?'bg-slate-400':'bg-amber-500')}}"></span>{{$d->status->value}}</span></td><td><div class="flex items-center gap-2"><span class="grid size-8 place-items-center rounded-full bg-slate-100 text-xs font-bold text-slate-600">{{mb_substr($d->creator->name,0,1)}}</span>{{$d->creator->name}}</div></td><td class="text-right"><a class="btn-ghost px-3 py-2" href="{{route('documents.show',$d)}}">ดูรายละเอียด <svg class="size-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-width="2" d="m9 18 6-6-6-6"/></svg></a></td></tr>@empty<tr><td colspan="5" class="empty-state">ยังไม่มีเอกสารประเภทนี้</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-5 py-4">{{$documents->links()}}</div></div>
@endsection
