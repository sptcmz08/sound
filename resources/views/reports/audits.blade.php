@extends('layouts.app') @section('title','Audit Log') @section('header','Audit Log')
@section('content')
<div class="mb-6"><h2 class="page-title">Audit Log</h2><p class="page-subtitle">บันทึกการเปลี่ยนแปลงสำคัญเพื่อการตรวจสอบย้อนหลัง</p></div>
<div class="table-shell"><div class="table-wrap"><table class="data-table"><thead><tr><th>วันเวลา</th><th>ผู้ใช้งาน</th><th>Action</th><th>Entity</th><th>ข้อมูลใหม่</th><th>IP Address</th></tr></thead><tbody>@forelse($rows as $r)<tr><td class="whitespace-nowrap text-xs">{{$r->created_at->format('d/m/Y H:i:s')}}</td><td><div class="flex items-center gap-2"><span class="grid size-8 place-items-center rounded-full bg-slate-100 text-xs font-bold">{{mb_substr($r->user?->name??'S',0,1)}}</span>{{$r->user?->name??'ระบบ'}}</div></td><td><span class="badge-blue">{{$r->action}}</span></td><td class="font-mono text-xs">{{$r->entity_type}} #{{$r->entity_id}}</td><td><code class="block max-w-md truncate rounded-lg bg-slate-100 px-2 py-1 text-xs">{{json_encode($r->new_values,JSON_UNESCAPED_UNICODE)}}</code></td><td class="font-mono text-xs">{{$r->ip_address}}</td></tr>@empty<tr><td colspan="6" class="empty-state">ยังไม่มี Audit Log</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-5 py-4">{{$rows->links()}}</div></div>
@endsection
